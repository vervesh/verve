# Enable embedded UI (set to "true" to serve the frontend from the server)
UI=false

# 32-byte hex key for encrypting secrets (required for GitHub token storage)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# Anthropic API key for AI-powered epic planning (optional)
ANTHROPIC_API_KEY=

# PostgreSQL connection (omit POSTGRES_HOST_PORT to use SQLite instead)
# For dev with docker compose, leave POSTGRES_HOST_PORT commented out to use SQLite
# POSTGRES_USER=verve
# POSTGRES_PASSWORD=verve
# POSTGRES_HOST_PORT=postgres:5432
# POSTGRES_DATABASE=verve

# SQLite file directory (only used when Postgres is not configured)
# Set to "data/" to persist SQLite data locally
# Omit to use in-memory SQLite (data will not persist)
SQLITE_DIR=data/

# How long to keep task logs before automatically deleting them (Go duration format).
# Examples: 168h (7 days), 720h (30 days), 2160h (90 days)
# Omit or set to 0 to keep logs forever.
# LOG_RETENTION=168h

# Custom Claude models (comma-separated, optional)
# Each entry is "value" or "value:label". If omitted, defaults to haiku,sonnet,opus.
# Example: CLAUDE_MODELS=haiku,sonnet,opus
# Example with labels: CLAUDE_MODELS=claude-3-haiku:Haiku,claude-3-sonnet:Sonnet,claude-3-opus:Opus
# CLAUDE_MODELS=

# Disable TLS certificate verification for GitHub API calls (default: false)
# WARNING: Only enable this in networks with TLS-intercepting proxies (e.g. corporate
# environments). This disables certificate validation for all server-side GitHub API
# requests (PR status checks, repo listing, etc.), making connections vulnerable to
# man-in-the-middle attacks. Do not enable in production unless absolutely necessary.
# GITHUB_INSECURE_SKIP_VERIFY=false
